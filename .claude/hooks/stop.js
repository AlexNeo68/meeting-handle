// Полный цикл (issues → тесты → фиксы → PR → review) теперь управляется
// .claude/ralph-start.js. Этот hook не должен перезапускать сессии — иначе
// получатся дублирующие/рекурсивные прогоны.
console.log('ℹ️ stop hook: цикл управляется ralph-start.js — перезапуск не требуется.')
process.exit(0)
